import { lineBreaks } from '../../../helpers/line-breaks';
import { module, test } from 'qunit';

module('Unit | Helper | line breaks');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = lineBreaks(42);
  assert.ok(result);
});
